# Copy to .streamlit/secrets.toml (gitignored) or set the same name as an
# environment variable. On Streamlit Community Cloud, paste it into
# Settings > Secrets instead; the file is never part of the repository.
GEMINI_API_KEY = "your-key-here"
